# Skill：讲解仓库

## 何时使用
用户问「这是什么项目」「怎么运行」「结构是怎样的」「给我介绍一下仓库」。

## 步骤
1. 用 `list_dir` 看顶层目录
2. 优先 `read_file` 阅读 `README.md`（如有）
3. 找入口：如 `main.py`、`cli.py`、`package.json`、`pyproject.toml`
4. 用中文说明：项目做什么、怎么安装/启动、关键目录各是干什么的
5. 若用户要打开文件，可用 `open_path`

## 禁止
- 没有读文件就臆造功能
- 一次把所有文件内容都读进对话（先摘要，再按需深挖）
