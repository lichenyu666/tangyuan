# Skill：修复报错

## 何时使用
用户贴了报错日志，或说「修好这个错」「测试不过」「帮我 debug」。

## 步骤
1. 仔细读报错，找出文件名、行号、错误类型
2. 用 `read_file` 打开相关代码，不要猜
3. 用 `apply_patch` 做最小修改（优先）；仅在新建文件或大面积重写时才用 `write_file`
4. 用 `run_shell` 复现/验证（如有测试就跑测试）
5. 用中文总结：根因、改了什么、怎么验证的

## 禁止
- 大范围无关重构
- 编造没读过的 API
- 删除 `.env` 或用户数据
- 能 patch 却整文件覆盖
