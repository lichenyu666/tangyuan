# Skill：生成 PPT

## 何时使用
用户说「做个 PPT」「生成幻灯片」「做成演示文稿」，或明确要 `.pptx`。

## 步骤
1. 先弄清主题与材料：若有 `@文件` 或路径，先读内容；没有就根据对话整理大纲
2. 列出 3～7 页大纲（每页一个重点）
3. 调用 `create_pptx`：写清 `path`（如 `output/xxx.pptx`）、`title`、`slides`
4. 用 `open_path` 打开生成的 pptx（若用户在 Mac 上）
5. 告诉用户文件保存在哪

## 禁止
- 没材料就编造大量虚假数据
- 一次生成几十页空话 PPT
